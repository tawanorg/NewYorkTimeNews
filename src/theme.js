// color
const groundBlack = '#292929';
const white = '#FFFFFF';

const fontFamilyBase = `'Merriweather', serif`;

const themes = {
    light: {
        // common
        primary: groundBlack,
        secondary: white,
        borderColor: '#eaeaea',
        // typography
        fontColor: groundBlack,
        fontFamily: fontFamilyBase,
    },
    dark: {},
}

export default themes;