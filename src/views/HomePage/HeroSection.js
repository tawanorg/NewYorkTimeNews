import { Section } from 'components/Layouts';
import styled from 'styled-jss';

const StyledRow = styled(Section)({
    border: '1px solid #ebefb9',
    borderRadius: 4,
    padding: '15px',
    background: '#fdffdd',
});

export default StyledRow;